sweep_name: epropplus_no_post_zero
base_config: config.yaml
output_dir: /projects/eprop_motor_net/
nest_module: src/motor_controller_model/nestml_neurons/nestml_install/motor_neuron_module.so
force_retrain: true

# Isolates the effect of training.time_post_ms=0: the post-movement phase
# used to zero the planner input trajectory regardless of the actual
# target position (see minjerk_dynamics.trajectory.generate_trajectory),
# which taught the network a "fake zero" at the end of every sequence
# instead of holding at the true target angle. Removing it tests whether
# that artifact was contributing to the low-activity / zero-converged
# failure mode.
#
# Deliberately no static (90->90) trajectory here, unlike
# epropplus_static_trajectory -- this experiment isolates the fake-zero
# fix on its own, moving-only pool.

runs:

  - name: no_post_zero_seed1234
    overrides:
      simulation.rng_seed: 1234
      task.n_iter: 1000
      plotting.spikes_plot_n_sequences: 6
      training.sampling.strategy: shuffled_epochs
      rbf.num_centers: 25
      rbf.sdev_hz: 1500.0
      training.time_post_ms: 0.0
      training.trajectories: &no_post_zero_trajectories
        # ±2°, moving only (no static entry)
        - {init_angle_deg: 88.0, target_angle_deg: 138.0}
        - {init_angle_deg: 88.0, target_angle_deg: 140.0}
        - {init_angle_deg: 88.0, target_angle_deg: 142.0}
        - {init_angle_deg: 88.0, target_angle_deg: 18.0}
        - {init_angle_deg: 88.0, target_angle_deg: 20.0}
        - {init_angle_deg: 88.0, target_angle_deg: 22.0}

        - {init_angle_deg: 90.0, target_angle_deg: 138.0}
        - {init_angle_deg: 90.0, target_angle_deg: 140.0}
        - {init_angle_deg: 90.0, target_angle_deg: 142.0}
        - {init_angle_deg: 90.0, target_angle_deg: 18.0}
        - {init_angle_deg: 90.0, target_angle_deg: 20.0}
        - {init_angle_deg: 90.0, target_angle_deg: 22.0}

        - {init_angle_deg: 92.0, target_angle_deg: 138.0}
        - {init_angle_deg: 92.0, target_angle_deg: 140.0}
        - {init_angle_deg: 92.0, target_angle_deg: 142.0}
        - {init_angle_deg: 92.0, target_angle_deg: 18.0}
        - {init_angle_deg: 92.0, target_angle_deg: 20.0}
        - {init_angle_deg: 92.0, target_angle_deg: 22.0}
      # Standalone inference test: spread across the ±2° pool, matching
      # the moving-only training pool (no static case here).
      inference.trajectories: &no_post_zero_inference
        - {init_angle_deg: 90.0, target_angle_deg: 140.0}
        - {init_angle_deg: 90.0, target_angle_deg: 20.0}
        - {init_angle_deg: 88.0, target_angle_deg: 138.0}
        - {init_angle_deg: 92.0, target_angle_deg: 22.0}

  - name: no_post_zero_seed2345
    overrides:
      simulation.rng_seed: 2345
      task.n_iter: 1000
      plotting.spikes_plot_n_sequences: 6
      training.sampling.strategy: shuffled_epochs
      rbf.num_centers: 25
      rbf.sdev_hz: 1500.0
      training.time_post_ms: 0.0
      training.trajectories: *no_post_zero_trajectories
      inference.trajectories: *no_post_zero_inference

  - name: no_post_zero_seed3456
    overrides:
      simulation.rng_seed: 3456
      task.n_iter: 1000
      plotting.spikes_plot_n_sequences: 6
      training.sampling.strategy: shuffled_epochs
      rbf.num_centers: 25
      rbf.sdev_hz: 1500.0
      training.time_post_ms: 0.0
      training.trajectories: *no_post_zero_trajectories
      inference.trajectories: *no_post_zero_inference
